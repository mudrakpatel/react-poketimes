import React from 'react';

const Contact = (props) => {
    // setTimeout(() => {
    //     props.history.push('/');
    // }, 3000);
    
    return(
        <div className="container">
            <h4 className="center">Contact</h4>
            <p>Contact page to get in touch.</p>
        </div>
    );
};

export default Contact;